name: Test Login
on: workflow_dispatch

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      - name: Intentar login con el secret
        run: |
          USER="mi_usuario"
          PASS="${{ secrets.MI_SECRET }}"
          echo "Probando login con $USER"
          curl -u "$USER:$PASS" https://api.mi-servicio.com/login
