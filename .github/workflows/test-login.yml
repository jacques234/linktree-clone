name: Test Login
on: workflow_dispatch

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      - name: Intentar login con el secret
        run: |
          username="AAMLA"
          password="password123"
          echo "Probando login con $USER"
          curl -u "$USER:$PASS" https://assessment-lit-accidents-strengths.trycloudflare.com/api/login
